{
  "name": "COMPASS — démo NIS 2 / ReCyF",
  "build": {
    "dockerfile": "../Dockerfile"
  },
  "overrideCommand": true,
  "postStartCommand": "python serve.py > /tmp/compass.log 2>&1 &",
  "forwardPorts": [8000],
  "portsAttributes": {
    "8000": {
      "label": "COMPASS — interface web",
      "onAutoForward": "openPreview"
    }
  }
}
